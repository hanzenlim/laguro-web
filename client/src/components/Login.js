import React, { Component} from 'react';
import './Login.css';

class Modal extends Component{

  render() {
    return (<a id="google" className="login waves-effect">Log in with Google</a>);
  };
};

export default Modal;
